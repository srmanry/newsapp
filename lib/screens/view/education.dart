import 'package:flutter/material.dart';

class EducationNews extends StatelessWidget {
  const EducationNews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Education Notince"),
      ),
    );
  }
}
