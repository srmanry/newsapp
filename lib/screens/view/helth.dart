import 'package:flutter/material.dart';

class HelthNews extends StatelessWidget {
  const HelthNews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('HELTH NEWS'),
      ),
    );
  }
}
